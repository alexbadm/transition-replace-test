import { FC } from "react";
import "./TestComponentAlpha.css";

export const TestComponentAlpha: FC = () => (
  <div className="alpha">
    <div>alpha test component header</div>
    <div>alpha text</div>
  </div>
);
